DATABASE_URL="postgresql://unicom:unicom@localhost:5433/unicom?schema=public"
AUTH_SECRET="change-me-use-openssl-rand-base64-32"
# 生产环境（https://uni.orblead.com）见 deploy/env.production.example
# 本地先：docker compose up -d && npm run db:push
PERSONNEL_FILE="./data/罗湖联通业务员名单.xlsx"
ORDERS_FILE="./data/业绩登记模版-上传数据系统.xlsx"
