# 复制为服务器 /opt/leadspace-unicom/.env 后填写（不要提交到 git）
# cp deploy/env.production.example .env

POSTGRES_USER=unicom
POSTGRES_PASSWORD=change-me-use-openssl-rand
POSTGRES_DB=unicom

AUTH_SECRET=change-me-use-openssl-rand-base64-32
AUTH_URL=https://uni.orblead.com

# 首次部署可选：RUN_DB_SEED=1 ./deploy/server-deploy.sh
